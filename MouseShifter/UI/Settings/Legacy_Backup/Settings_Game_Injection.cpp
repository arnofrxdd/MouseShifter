// This fragment's checkboxes (Block Mouse/XInput) are now handled by the registry in Settings_VariablesAndTitles.cpp.
// The custom instruction text remains for reference or can be moved to a tooltip.
/*
        PointF instructionPos((REAL)mouseBlockCheckboxRect.left, (REAL)(mouseBlockCheckboxRect.bottom + 5 + settingsScrollOffset));
        SolidBrush instructionBrush(Color(150, 150, 150));
        Font smallInstructionFont(&fontFamily, 10, FontStyleRegular, UnitPixel);
        graphics.DrawString(L"Hold RMB to use mouse", -1, &smallInstructionFont, instructionPos, &instructionBrush);
*/
